
#Push the changes to master
echo "pushing changes to master..."
git add .
git commit -m "new update"  
git push origin master:master

echo "Done\mBuilding updates...."
npm run build
mv dist ./dist22

echo "Done\nmoving new build to gh-pages"
git switch gn-pages
mv ../dist22/* .
ls
echo "Done\npushing to gihub pages (gh-pages branch)"
git add .
git commit -m "new updates"
git push origin gh-pages"gh-pages
